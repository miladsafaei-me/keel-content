# TODO

This file is the single source of truth for pending, follow-up, and deferred work on this project. See CLAUDE.md for the tracking rule.

Guidelines:
- Add a task here as soon as it's identified — with priority, prerequisites/dependencies, and enough context to pick it up cold.
- Group by priority: P0 (urgent / blocking / production risk), P1 (next up), P2 (backlog / nice-to-have).
- Note real dependencies explicitly ("Blocked by: ...", "Requires: ...").
- Delete a task from this file the moment it's done. This file only ever holds what's left.

## P1 — Next up
- [ ] **Audit the remaining lazily-resolved hooks for the same partial-write hazard `content_relink` just hit.** `refresh_rendered_hook` resolved per post INSIDE the relink write loop, so Martiland — which had not configured it and fell through to the package default `blog.tasks.refresh_article_rendered` (SignalBots' app layout) — saved post #1 and then died on `ModuleNotFoundError`, leaving a live 339-post corpus half-rewritten. Fixed for that one command 2026-08-22 by resolving it once up front (`host.resolve_refresh_article_rendered`). The same shape exists wherever a `_cfg`-resolved hook is first touched mid-mutation: `prepare_storage_hook` / `markdown_html_hook` in the same loop, and the publisher path. Sweep them and add a pre-flight resolve to every command that writes more than one row.
- [ ] **The glossary AUTHORING/PERSISTING commands still assume keel-cms's Tag shape.** `author_glossary_terms` and `persist_glossary_terms` write `is_term=True` and read `.name` directly, and `persist_glossary_terms`'s emitted migration hardcodes `Tag.objects.filter(is_term=True)`. The intent-gate read path was made shape-agnostic 2026-08-21 (`host.glossary_term_queryset()` / `host.term_name()`), but the write path was left alone because only SignalBots authors terms through the pipeline today. The moment a host with a dedicated term model (Prop Firm Review's `core.PropTerm`) wants to author terms through it, these break the same way the exporter did with `FieldError: Cannot resolve keyword 'is_term'`.
- [ ] **`keel_cms.Post` implements no `get_absolute_url`, so every keel-cms host MUST wire `KEEL_CONTENT["post_url_hook"]`.** `keel_cms.Tag` has one (it reverses `keel_cms:trading_glossary_term` / `keel_cms:tag_detail`); `Post` has none at all, so keel-content's fallback chain has nothing to fall back on and `host.post_url` returns `""` — which now correctly refuses rather than guessing, but means the hook is mandatory rather than optional for Binary, Martiland, Sarmayeh and Prop. Giving `Post` the same guarded `reverse()` treatment `Tag` already has would make the hook optional again for hosts that do register `keel_cms:` route names. This is a **keel-cms** change, not a keel-content one. Found 2026-08-21 in the first-adopter-default sweep.
- [ ] **Sweep leftovers: three lower-severity first-adopter defaults, deliberately not changed.** (a) `core/config.py::_load_db_overrides` and `core/claude_client.py` import `core.models.AiSetting` by a hardcoded path, not through `_cfg` — both swallow the ImportError and degrade to no-overrides, so a host without that model is merely silently un-configurable rather than broken; making it a dotted setting would be consistent with every other seam. (b) `host.py` lines ~177/190 import `core.services.ai_settings` directly with no `_cfg` override, inside the seam module itself. (c) `core/external_links.py`'s fast-lane allowlist is a trading/finance domain list baked as the package default (investopedia, babypips, stockcharts, xe.com…); it is additive and `KEEL_CONTENT["external_domains"]` extends it, but for a non-finance consumer like Martiland the default carries topical bias the package should not hold an opinion about. (d) `keel_cms.Tag` carries a field literally named `signalbots_context`, written by `author_glossary_terms` — a host name in a shared schema; also a keel-cms change.
- [ ] **The hero palette is bound at import time, the hero wordmark per call — pick one.** `core/hero/tokens.py` evaluates `GREEN = _brand("accent")` and its siblings at module import, so a host's `KEEL_CONTENT["brand"]` palette only applies if it was set before the module loaded and can never change afterwards; `core/hero/chrome.py` resolves `_brand("wordmark")` inside the function on every call, so it follows settings live. Found 2026-08-21 while un-redding `test_hero`: `override_settings` moves the wordmark but cannot move the accent, and the connector test had to read the resolved `GREEN` token instead of pinning a colour. In production both paths happen to work (settings load first), so this is a testability and consistency defect rather than a live bug — but a host that ever needs two palettes, or a test that needs to assert one, hits a wall. Make tokens resolve per call like chrome does, or document the freeze and give `tokens` an explicit reload seam.
- [ ] **One test module cannot run: `test_content_relink_cross_cluster` needs a test database, and no host can currently build one.** Binary Option Trading dies in its own `core.migrations.0079_rewrite_glossary_and_broker_urls` (`column "content" does not exist`) and SignalBots dies on a broken migration graph (`blog.0110` references a nonexistent `blog.0109_load_proposed_term_visuals`). Both are in-flight work from other sessions in the shared checkouts, unrelated to this package. The other 14 modules (188 tests) run green without a DB. Re-run this module once either host's migrations are consistent again.
- [ ] **Crawl-map route: stages 2 and 3 are not written yet.** `keel_content/crawlmap/` currently ships only stage 1, `structure.py` (stored HTML → structured Markdown + a `PageSignals` record carrying headings, tables, FAQ, author/dates, schema types, and the internal-link graph). Landed 2026-08-19 for Binary Option Trading's competitor mapping (4 sites, ~3k English pages). Still to build, in order: **(2) classify** — group pages into page types and clusters from URL shape + heading/title n-grams + a host-supplied vocabulary (the only business-aware input; comes through `KEEL_CONTENT` config, never hardcoded), and rank hub-vs-spoke from the internal-link graph; **(3) atlas** — emit the tiered artifacts a planning stage reads (small always-loaded overview → per-cluster index → per-page skeleton → raw page) plus a query CLI so a stage retrieves a cluster or a skeleton instead of grepping the corpus. The tiering is the whole point of the route: see CLAUDE.md's "The crawl-map route" section for the constraints that must not be simplified away (deterministic/no model calls; maps *all* page types, not just blog). Consumer-side driver meanwhile lives at `binaryoptiontrading/backend/scripts/rebuild_crawl_structure.py` and should collapse into a management command here once stages 2-3 exist.
- [ ] README.md's "Status" section (bottom of the file) is frozen at v0.1.5 while `pyproject.toml` is at v0.1.49 (44 releases of drift, last touched commit 8553b66 on 2026-07-29). Rewrite it to describe current behavior instead of the v0.1.4/v0.1.5 changelog narrative. (The one open follow-up sentence about `agent-author-brief.md`/`cluster-internal-links.md` describing the pre-v0.1.4 linking flow was re-verified and resolved 2026-08-18, `intent-gate-rollout.md` P1.3 — both prompts now describe the brief-stage `glossary_targets`/directed `link_plan`/`anchor_concept` contract accurately. The version-number rewrite itself is still open.)
- [ ] The prose-authoring/visual-plan split (`generate.workflow.js`'s "Visual plan" phase) has no default prompts: `visualPlanPath`/`visualRewritePath` default to `content-pipeline/prompts/visual-plan.md` / `visual-rewrite.md`, but neither file exists anywhere in `prompts_default/` — a host relying purely on defaults gets a Visual plan stage that tries to read a nonexistent file. Found 2026-08-18 while doing P1.3 (`intent-gate-rollout.md`). Two sibling prompt fragments are also stale against this same decoupling and were left untouched (P1.3 was scoped to `agent-author-brief.md`/`cluster-internal-links.md` only): `brief-output-contract.md` still documents `body_markdown` as containing "each in-body visual as a fenced cp-component data block at its anchor" and says the author "emits `figure_requests` + their markers"; `brief-visual-system.md` (the fragment describing how to choose/emit `cp-component`/`figure_requests`/`image_requests`) is now orphaned — no default prompt reads it as its own contract, since `agent-author-brief.md` no longer does and the stage that should (`visual-plan.md`) doesn't exist. Needs: either write `visual-plan.md`/`visual-rewrite.md` as new default prompts (reusing `brief-visual-system.md`'s content) and update `brief-output-contract.md`'s bundle-shape description, or repoint `generate.workflow.js` at a different default — a real design decision, not a doc fix.
- [ ] **`ContentPublisher` advertises two methods the package never calls.** `core/publisher.py` declares `resolve_author_id(slug)` and `resolve_reviewer_id(slug)` on the Protocol, but a repo-wide grep finds zero call sites — and the reference adapter (`signalbots/backend/content_pipeline/keel_adapter.py`) does not implement either, yet works. So the documented contract is wider than the real one, and every new adopter either writes dead code or omits them and cannot tell whether their adapter is incomplete. Hit during Binary Option Trading's adoption 2026-08-19. Either wire them where author/reviewer resolution actually happens, or drop them from the Protocol and say in README's "Consume it" that `publish()` is the whole seam.
