# content_pipeline secrets — copy to project-root .env (NOT to content-pipeline/.env).
# The Django project's settings.py loads .env from the repo root, and the pipeline
# reads ANTHROPIC_API_KEY from there. Gemini key lives in core.AiSetting (DB).

ANTHROPIC_API_KEY=sk-ant-...
